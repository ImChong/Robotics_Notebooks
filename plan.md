# 软件工程改进计划

状态：阶段 A 已通过 PR #1820 合并；阶段 B 正文按页加载（#2、#16 详情端）已通过 PR #1852 进入 main，该 PR 四项 CI 检查通过；#4/#8/#9/#11/#23 部分完成，其余工作按下方投入收益顺序推进。本次仅更新计划与清单，不实施性能改动。

初次审查基线：`740f04209ea5accb8117a8e1dcc8c7c70ea30915`（2026-09-06）。
性能复核基线：`4b0320bf33d6a63cb71cadc5a59afcb6f02e237d`（2026-09-07）；线上资源测量与 Node 微基准见下文。
目标：保持 Python + 静态站架构，先修复可复现缺陷，再降低加载成本，补齐行为门禁，最后优化结构。
本文件是工程计划，不属于 wiki 知识页；保留原审查编号 1–35，追加 #36 首页活动摘要、#37 单页元数据与局部关系。

## 执行规则

- 先提交本计划并创建 PR，再实施。第一批交付阶段 A；后续阶段按依赖拆成可独立审查的 PR，避免把全部 35 项塞入一次发布。
- 每个完成项必须记录修改、验证结果与提交/PR；未验证、部分实现和受环境限制的工作不能标成完成。
- 每完成一个可独立验收的改进项就提交并推送一次，不累计成一个大提交；本次计划更新使用独立 PR。
- 保留现有页面 ID、链接和搜索入口。数据格式变更先确定迁移与回滚策略，再同时更新导出端和消费端。
- 提交前执行 `make ci-preflight`；修改脚本、前端和测试后补充相应行为测试，运行仓库要求的质量门禁。只提交任务相关派生文件，大 JSON 和 sitemap 仍在部署时生成。
- 更新本文件和对应前端 checklist。PR Checks 通过后才具备合并条件；本任务不自动合并或发布。

## 初次审查基线与测量边界（历史记录）

| 指标 | 审查基线 |
| --- | --- |
| 受版本控制文件 / 导出页面 | 8,424 / 3,527 |
| 图谱节点 / 边 | 3,493 / 32,071 |
| 搜索索引原始 / gzip | 66.03 MB / 16.45 MB |
| 详情全集 / 图谱侧栏索引原始 | 27.57 MB / 27.81 MB |
| 完整图谱 / 活动数据原始 | 5.42 MB / 3.63 MB |
| 自动化基线 | Python 431 项、648 子测试通过；搜索 40/40；Ruff、mypy、ESLint 通过 |

MB 为十进制。gzip 为当次线上响应测量，不代表其他环境。Node 单次搜索 JSON 解析约 2.35 秒、进程 JS 堆约 362 MB，仅作数据结构成本参考。尚无真机手机、Safari、弱网 FPS/LCP/INP 基线。当次云浏览器无法创建 WebGL 上下文，只可验证失败恢复，不能据此评价正常 GPU 的 3D 性能。

## 2026-09-07 性能复核与当前基线

结论：Python + 静态站路线及阶段划分仍成立；当前优先减少默认大数据加载、重复解析，再优化搜索结构与图谱规模。正文按页加载已经落地，不能把正文通道约 80% 的降幅视为整个详情页的加载降幅。

线上站点：<https://imchong.github.io/Robotics_Notebooks/>。本轮下载的 `main.js`、`sw.js`、`style.css` 与复核提交逐字节一致；首页 HTML 的互链 fallback 数为 34,034，复核代码/线上图谱为 34,046，不能宣称整个线上产物与同一 SHA 完全一致。页面目录与搜索均为 3,857 项；图谱为 3,823 节点、34,046 条边。

| 线上资源 | 原始字节 | gzip 响应字节 | 当前加载行为 |
| --- | ---: | ---: | --- |
| `search-index.json` | 68,650,457 | 16,821,517 | 首页 idle 自动预取并解析；SW 同时列入预缓存 |
| `exports/index-v1.json` | 29,306,387 | 7,756,305 | 完整图谱加载后自动获取，含 `content_markdown` |
| `exports/site-catalog-v1.json` | 5,706,019 | 1,228,078 | 详情、路线等先取全站目录，再取当前正文 |
| `exports/link-graph.json` | 5,814,600 | 818,446 | 首页 Top-50、社区筛选、详情附属功能仍读取全图 |
| `exports/wiki-activity.json` | 3,784,665 | 540,269 | 首页只展示最多 5 条更新，却读取 147 天活动数据 |
| `exports/hub-rankings.json` | 1,619,281 | 258,249 | 完整榜单页使用，SW 默认预缓存 |
| `vendor/3d-force-graph.min.js` | 1,313,897 | 350,145 | 完整图谱页静态引入；首页已有按需加载 |
| `main.js` / `style.css` | 317,454 / 101,152 | 78,996 / 25,025 | 相比数据负担，单纯压缩或拆文件收益较低 |

测量口径：请求携带 `Accept-Encoding: gzip`，记录响应体长度，解压后记录原始长度；不含 HTTP 头，也不是浏览器总流量。线上资源 `Last-Modified` 为 2026-09-07 00:45:42–43 UTC。后续对比须固定同内容数据，不能用旧基线与持续新增内容直接计算优化收益。

本地实验（未修改站点代码）：

- 在 Node v24.19.0 中用线上索引做一次 `JSON.parse`，耗时约 2,088 ms；`--expose-gc` 下解析前后回收后堆占用差约 270.2 MB。该差值不含预先读取的 JSON 字符串，不是峰值或浏览器内存。
- 提取现有 `tokenizeQuery`、`substringScore`、`bm25Score`，执行全量扫描及排序：首次 `PPO` 约 1,242 ms，重复 `PPO` 约 11 ms；随后“强化学习”约 259 ms。包含惰性字符串缓存构建与运行时预热差异；不含网络、DOM 或浏览器调度，不能当作 INP 或稳定性能预算。
- 同份侧栏索引仅移除 `content_markdown`，原始体积 29,306,387 → 5,530,173 字节；同口径本地 Python gzip 为 7,662,545 → 995,563 字节。该实验验证精简潜力，不是已部署收益，也不能与线上 gzip 直接比较。
- 详情的社区、路线、机构徽标、局部图谱和近期关联更新有 5 处独立 `fetch`/`response.json()`。共享 Promise 可减少重复解析；HTTP 缓存可能复用响应，不能据此声称减少五倍网络流量。
- 本轮 `node --test tests/*.test.cjs` 33/33 通过；未测真机手机、Safari、弱网 LCP/INP、正常 GPU 帧率或真实浏览器 E2E。历史 checklist 的 Lighthouse 90+ 勾选不能充当当前基线。

证据入口：[默认缓存](docs/sw.js)、[页面与搜索加载](docs/main.js)、[首页预览](docs/mini-graph.js)、[完整图谱](docs/graph.html)、[详情依赖](docs/detail.html)、[索引生成](scripts/build_search_index.py)。

## 下一批执行顺序：按投入收益比

排序是当前数据规模下的工程判断；工时为包含针对性回归的粗估，并非承诺。收益有重叠，不相加。阶段表继续作为完整范围清单；本表决定近期顺序。

| 顺序 | 编号 | 实施范围与主要收益 | 预计投入 |
| --- | --- | --- | --- |
| 1 | #4、#12 | 同时移除 SW 大资源预缓存与首页无条件 idle 搜索预取；搜索意图触发加载。普通浏览可避免 16.82 MB 压缩搜索索引及其解析，外壳离线完整性同步处理 | 0.5–1 天 |
| 2 | #8 | 共享详情各模块的图谱 Promise 与解析对象，5 处解析降到 1 次；失败释放 Promise，图布局不得污染共享原始数据 | 0.5 天 |
| 3 | #3 | 导出无正文侧栏元数据，按节点交互获取必要信息；保留旧导出供旧客户端使用 | 0.5–1 天 |
| 4 | #36 | 首页使用最近 5 条新增摘要；统计展示不再等待活动全集，完整活动留给更新记录页 | 0.5 天 |
| 5 | #5 | 构建时导出 Top-50 预览子图与独立社区映射；首页预览与社区筛选不再取全图 | 1–1.5 天 |
| 6 | #9 | 完整图谱按需加载 3D，详情按内容加载 Mermaid/KaTeX；保留首页已有 3D 懒加载与失败恢复 | 1–2 天 |
| 7 | #1、#7 | 精简搜索索引、倒排候选集、Worker 内加载与解析；先解决冷启动和全量扫描，再优化 Top-K；沿用 #18/#19 查询样例避免排名退步 | 3–5 天 |
| 8 | #37 | 详情使用单页元数据/局部邻接数据；减少读取全站目录和图谱，保留内链预览与路线兼容 | 2–4 天 |
| 9 | #6 | 默认减少真正参与力模拟和 DOM 渲染的节点/边；避免只改透明度仍计算全图，再评估渲染器迁移 | 2–4 天起 |

建议首批完成顺序 1–4（约 2–3 天粗估）；之后推进首页子图、依赖按需加载与搜索结构改造。关闭 idle 预取会把等待移到首次搜索，应同时记录首次搜索延迟并保留清晰的加载/重试状态；`requestIdleCallback` 不会让解析自动离开主线程，Worker 也不会减少传输体积。

#35 提升至近期 P1：随每项变更记录固定数据下首页冷缓存/热缓存、首次/重复搜索、详情切换与图谱筛选的资源请求、解析次数、长任务及可测交互指标。首次安装、已有 SW 升级和离线分别验收；设备、浏览器、节流配置与样本次数必须记录。真机/GPU 缺失时明确留空，不沿用历史得分。#11/#23/#24 的相关版本、数据契约和行为回归随数据格式修改落地，不等待阶段 C 全部结束。CI 去重、大规模模块重构与完整渲染器迁移后置。

## 阶段 A：明确缺陷与失败恢复

依赖：无。第一批实现范围如下，完成后单独交付审查。

| 状态 | 编号 / 优先级 | 工作 | 验收标准 |
| --- | --- | --- | --- |
| 已完成 | 17 / P1 | 修正 sitemap 部署根路径 | 所有 URL 使用真实 Pages 根路径；解析 XML 校验路由、ID 与转义；不出现多余 `/docs/` |
| 已完成 | 10 / P1 | Service Worker 缓存命名与清理隔离 | 只删除本项目旧缓存；同源其他项目缓存保留；自动化复现升级清理 |
| 已完成 | 13 / P1 | 搜索加载失败可恢复 | 网络错误、HTTP 错误和超时后可重试；并发加载复用请求；成功后恢复搜索 |
| 已完成 | 14 / P2 | 防止过期搜索回填 | 输入清空、Esc、快速更换查询时，旧异步结果不得覆盖当前界面 |
| 已完成 | 15 / P2 | WebGL 初始化失败恢复 2D | 同步异常和加载失败均保留/恢复 2D；按钮、存储状态一致；提示可读；可再次操作 |
| 已完成 | 16 / P2（搜索端） | 搜索报错提供读者操作 | 提供重试入口，不要求读者运行 Python；详情端已随阶段 B 完成，见下表 |

验证：sitemap Python 行为测试；Service Worker 生命周期与搜索竞态 JS 行为测试；WebGL 失败路径行为验证；相关前端 lint；完整 preflight。可用时补浏览器交互验证，缺失的真实 GPU 验证须明示。

## 阶段 B：降低默认加载成本

依赖：A；数据契约先行，旧页面与缓存的过渡和回滚须随实现说明。

| 状态 | 编号 / 优先级 | 工作 | 验收标准 |
| --- | --- | --- | --- |
| 待办 | 1 / P1 | 搜索索引精简与倒排候选检索 | 保存同内容基线的原始/gzip体积、解析时间、内存；索引目标减少至少 50%；核心查询无退步 |
| 已完成 | 2 / P1 | 轻量目录 + 单页正文 | 打开单篇详情不下载全站正文；深链、路线、内链预览正常；缺页与请求失败可恢复 |
| 待办 | 3 / P1 | 图谱侧栏只加载元数据 | 侧栏索引无 `content_markdown`；标题、标签、关系、来源功能完整 |
| 部分完成 | 4 / P1 | 默认只预缓存外壳与小数据，按搜索意图预取 | 已移除两份正文全集；剩余搜索索引、全图、榜单、活动全集及首页无条件 idle 预取待移除；搜索意图触发加载，离线内容由用户主动选择 |
| 待办 | 5 / P2 | 导出首页预览子图与社区映射 | 首页 Top-50 与筛选不请求完整图谱；节点 ID 与完整数据一致 |
| 待办 | 7 / P2 | 搜索 Worker、候选集与 Top-K | 查询计算不阻塞主线程；结果与参考排名一致；取消与超时可恢复 |
| 部分完成 | 8 / P1 | 共享数据请求与解析 | 正文通道已有共享；图谱同 URL 只请求和解析一次仍待办；失败 Promise 可重试，模拟用副本不污染共享对象 |
| 部分完成 | 9 / P2 | 3D、公式、Mermaid 按需加载 | 首页 3D 已按需；完整图谱 2D 初始不加载 3D 库、无对应内容页面不请求公式/图表组件仍待办 |
| 部分完成 | 11 / P1 | 构建版本清单或内容哈希 | 正文哈希与 404 刷新目录已实现；HTML、JS、其余 JSON 配套以及升级中断、旧标签页、回滚一致性仍待验收 |
| 待办 | 12 / P2 | 必要与可选离线资源分开 | CSS 等外壳完整；可选资源失败不阻止安装；离线首页与已缓存详情可用 |
| 已完成（行为测试） | 16 / P2（详情端） | 详情错误提供重试与原文入口 | PR #1852：404、超时、断网可重试或返回首页搜索，目录可用时提供原文入口；真实浏览器验收待补 |
| 待办 | 36 / P1 | 首页最近更新使用轻量摘要 | 首页只取最多 5 条新增摘要，包含回填必需字段；不请求活动全集，统计不等待完整时间线；更新页保持完整记录 |
| 待办 | 37 / P2 | 单页元数据与局部关系 | #2 后续增量：详情无需全站目录/全图即可展示正文及徽标、小图；深链、内链预览、路线、失败恢复与旧客户端兼容 |

性能目标是待验证的验收目标，不是已实现收益。若准确率或兼容性与体积目标冲突，在 PR 中记录取舍和测量再调整计划。

## 阶段 C：数据正确性与质量门禁

依赖：A；浏览器测试可先接入，数据契约测试随 B 同步落地。

| 状态 | 编号 / 优先级 | 工作 | 验收标准 |
| --- | --- | --- | --- |
| 待办 | 18 / P2 | 英文 token 边界与中文匹配分离 | `PPO` 不把 `Multi-Support` 标成标题命中；缩写、中文、混合查询均有样例 |
| 待办 | 19 / P1 | 网页与 CLI 共用排序规则/样例 | 定义页优先、占位页降权在浏览器实际结果中成立；记录允许的差异 |
| 待办 | 20 / P2 | 保留规范化内容状态 | planned/stub/draft 不统一变成 active；展示和排序可识别；未知值有明确策略 |
| 待办 | 21 / P2 | 区分仓库引用与代码开放状态 | 状态附证据和核查日期；不从 `has_repo` 自动推断可运行开源 |
| 待办 | 22 / P2 | 正文与分享元信息预渲染 | 无 JS 可读主要内容；title、description、canonical、分享标签按页面生成 |
| 部分完成 | 23 / P2 | 精确导出一致性检查 | 正文目录的精确 ID、哈希、镜像和 v1 无损重建已实现；搜索、图谱及后续精简格式继续补字段/引用完整性与破坏样本门禁 |
| 待办 | 24 / P1 | 关键交互接入浏览器 CI | 搜索→详情、路线、图谱筛选、缓存升级与断网恢复测试实际行为 |
| 待办 | 25 / P2 | 扩大自有 JS lint 范围 | 覆盖自有 JS 与验证脚本；vendor 排除；逐步抽出内联逻辑 |
| 待办 | 26 / P2 | 按模块建立有效覆盖要求 | 导出、图谱、渲染有行为测试；覆盖率明确统计范围，避免仅靠字符串断言 |
| 待办 | 27 / P2 | 语义与降级测试分开 | 未运行真实语义模型时标 skip/降级，不报语义通过；核心查询必过 |
| 待办 | 28 / P1 | 部署依赖同提交质量门 | 测试失败不得部署；发布 SHA 与通过测试的构建产物一致 |
| 部分完成 | 35 / P1 | 性能与兼容性基线 | 已有线上体积与 Node 单次微基准；冷/热缓存、首次搜索、详情切换、浏览器长任务及真机/GPU基线待补，随近期优化记录 |

## 阶段 D：结构与构建效率

依赖：B/C 的相关行为门禁。#6 的默认规模控制可按近期顺序提前，不要求所有 B/C 工作完成；Canvas/WebGL 迁移与高成本结构重构仍先测瓶颈再决定。

| 状态 | 编号 / 优先级 | 工作 | 验收标准 |
| --- | --- | --- | --- |
| 待办 | 6 / P1 | 图谱分层、局部展开与渲染评估 | 大图默认控制实际参与模拟和 DOM 渲染的节点/边，不能仅降低透明度；记录同步 warmup、DOM、tick 与交互延迟；Canvas/WebGL 迁移由测量决定，保留可用降级 |
| 待办 | 29 / P2 | CI 去重、产物复用与改动范围 | 同提交不重复运行等价重任务；内容改动仍覆盖必要导出和部署检查 |
| 待办 | 30 / P2 | 并发导出后重新生成 | 模拟远端新增内容后，重试产物包含最新内容；不只 rebase 旧派生文件 |
| 待办 | 31 / P2 | 一次扫描与 Git 状态索引、测试 fixture | 减少逐文件 Git 子进程与重复全库构建；结果与旧检查相同，记录耗时差异 |
| 待办 | 32 / P2 | 拆分搜索、渲染、详情、图谱状态与 lint | 按职责渐进拆分；接口明确；每次拆分通过行为回归 |
| 待办 | 33 / P2 | 锁定构建依赖、登记 vendor | 干净环境可复现；第三方版本、来源、更新检查可追踪 |
| 待办 | 34 / P2 | 导入路径、响应大小与原子写入 | 拒绝路径穿越与超限响应；失败不留下半文件、不覆盖已有资料 |

## 交付记录

- 计划提交：覆盖审查编号 1–35，阶段 A 为首批实施范围；尚未宣称任何修复完成。
- 每阶段追加：PR/commit、完成编号、测试命令与结果、性能对比、遗留限制。

- 编号 17：修正 sitemap 根路径与 ID/XML 转义；新增 3 项 Python 路由行为测试。独立提交 `fix(sitemap): 修正部署路径并验证站点路由`；PR #1820。
- 编号 17 验证：3 项测试通过；全量 3,497 条 sitemap 路由存在；Ruff/格式通过；`make ci-preflight` 搜索 40/40、导出 12/12 通过。

- 编号 10：缓存删除、读取与请求作用域隔离；3 项 Node 生命周期/请求测试接入 CI。独立提交 `fix(cache): 隔离项目缓存与请求作用域`。
- 编号 10 验证：3/3 Node 行为测试通过；`make ci-preflight` 搜索 40/40、导出 12/12 通过。

- 编号 13（含编号 16 搜索端）：请求失败后释放 Promise，30 秒超时可重试；社区数据也可降级和重试；提供读者可点击的重试入口。独立提交 `fix(search): 支持加载失败重试与超时恢复`。
- 编号 13 验证：累计 7/7 Node 行为测试、ESLint 通过；`make ci-preflight` 搜索 40/40、导出 12/12 通过。编号 16 的详情页部分仍待阶段 B。

- 编号 14：输入事件立即使旧查询失效，成功/失败回调只渲染最新请求；清空与 Esc 取消延迟查询。独立提交 `fix(search): 阻止过期查询覆盖当前结果`。
- 编号 14 验证：累计 12/12 Node 行为测试、ESLint 通过；`make ci-preflight` 搜索 40/40、导出 12/12 通过。Python 全量 434 项、648 子测试通过。

- 编号 15：3D 库缺失、创建失败、WebGL 抛错和部分初始化清理失败均恢复 2D，同步按钮、提示与保存状态，允许再次尝试。独立提交 `fix(graph): 初始化失败时完整恢复二维视图`。
- 编号 15 验证：累计 18/18 Node 行为测试通过；`make ci-preflight` 搜索 40/40、导出 12/12 通过。新增前端测试模拟网络、DOM 与渲染器边界，不等同真实浏览器 E2E；本轮未验证正常 GPU/Safari/手机性能。

- 阶段 A 已推送提交：sitemap `73828a1`；缓存隔离 `4abff3c`；搜索重试 `6be4729`；过期结果保护 `9f4a338`；WebGL 恢复 `b194469`。
- 集成：同步最新 main `6782f88c6`，保留新增知识及首页修复；Service Worker 冲突保留新版缓存日期与项目隔离配置。
- 同步验收：18/18 前端行为测试；Python 434 项、684 子测试；Ruff/格式、mypy、ESLint；`make ci-preflight` 搜索 40/40、导出 12/12 均通过。更新数据后的 sitemap 为 3,815/3,815 条有效部署路由。保留 main 的已提交活动/统计产物，最终 CI 在完整合并历史上重新生成验证。


## 阶段 B 交付：正文按页加载（编号 2）

基于 main `5dfd854bd57e6f39607f41107766f2fca2a833c0`，独立分支 `codex/per-page-content`；本项单独 commit、推送和创建 PR。

- 导出 `site-catalog-v1.json`：保留目录元数据、路径、摘要、关系、路线阶段和别名，移除所有详情正文。`content_url` 映射到 `page-content/<SHA-256>.json`，文件中保留原 ID（包括 π 等 Unicode ID）。
- 详情与路线只加载当前页正文；模块、技术地图与预览页只读目录。共享成功的请求 Promise；失败或超时清除缓存，页面可重试并返回首页搜索；目录已加载时也可打开原始 Markdown。
- 目录网络优先、离线回退缓存；正文按内容哈希缓存。部署交替导致正文 404 时刷新目录重试一次，持续失败则展示恢复操作。SW 不再默认预取 `site-data-v1.json` 和含全文的 `index-v1.json`。
- 保留原 `site-data-v1.json` / `index-v1.json` 全量导出供旧客户端与现有工具使用；新数据与旧数据随同一 Pages 构建发布。回滚本提交后重新构建并刷新客户端脚本即可恢复旧加载方式，无源内容迁移。
- 全库导出门禁验证精确 ID 集合、SHA-256、文件镜像与字段契约，并重建原 v1 数据逐字段比较；生成目录清理过期哈希文件，新增产物仍 gitignore。

同内容测量（3,855 页，十进制字节；gzip 是本地压缩，不是线上响应）：

| 数据 | 原始字节 | 本地 gzip 字节 |
| --- | ---: | ---: |
| 正文全集 | 29,038,719 | 7,649,622 |
| 目录 | 5,702,539 | 1,194,570 |
| 单页正文中位数 / 最大值 | 4,920 / 139,706 | 未测 |
| Centroidal Dynamics 正文 | 11,583 | 未测 |
| 主学习路线正文 | 111,994 | 未测 |

正文加载通道由全集变为目录加单页，原始体积约减少 80%。搜索、图谱及活动数据的其他请求不计入这个降幅，默认缓存全面瘦身（编号 4）仍待办；目录仍可进一步按功能精简。

专项验证：新增 13 项页面加载 / 深链 / 路线 / 内链预览行为测试，新增 2 项 SW 行为测试；前端总计 33/33 通过。新增 9 项 Python 契约测试通过，含全库 3,855 页无损重建。真实浏览器验收尚缺：本轮云浏览器以 `ERR_BLOCKED_BY_CLIENT` 拦截本地预览地址，未生成截图，不宣称 E2E、Safari、真机或 GPU 性能通过。`make ci-preflight` 已完整通过（搜索 40/40、导出 13/13）；Ruff、格式、mypy 与 ESLint 通过。全量 Python 与远端 CI 的最终结果见本项 PR，未完成前不标全量测试通过。

2026-09-07 回填：[PR #1852](https://github.com/ImChong/Robotics_Notebooks/pull/1852) 于 00:39 UTC 合并，提交 `0ac9f318e03e370277150bd20cdf98f662580f34`，合并提交 `207920efdea30fd34fc974f08e8e75fcd387f5c1`。该 PR 的 [Tests](https://github.com/ImChong/Robotics_Notebooks/actions/runs/34048317016)、[Search & Export Quality Check](https://github.com/ImChong/Robotics_Notebooks/actions/runs/34048316988)、[Wiki Lint](https://github.com/ImChong/Robotics_Notebooks/actions/runs/34048316960) 和 [CI PR Gate](https://github.com/ImChong/Robotics_Notebooks/actions/runs/34048316952) 均通过；浏览器/真机/GPU 测量缺口不因此关闭。

## 2026-09-07 计划更新交付

- 独立分支 `codex/refresh-performance-plan`：更新完成状态、线上性能基线和九项投入收益排序，追加 #36/#37，同步前端 checklist；本次不实施上述优化。
- 本次本地验证：`make ci-preflight` 完整通过（搜索 40/40、导出 13/13，覆盖 3,857 页契约），前端行为测试 33/33，`git diff --check` 通过。只提交两个计划文档，预检产生的无关日期/统计变化不纳入本次 PR；未新增全量 Python、浏览器或 GPU 验收结论。远端 CI 结果见本次 PR。
